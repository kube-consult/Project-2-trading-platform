<div class="container-fluid">
  <div class="row justify-content-between">
 
    <!-- Main -->
      <div class="container col-sm-6 bg-secondary text-white">
    <div class="row">
      <div class="col-sm-12 col-md-offset-3">

              {{!-- <p>Two buttons in a group:</p> --}}

              {{!-- <p>Two buttons in a group:</p> --}}
              <br>
    <a href="something" id="input-trade" class="button1">Trade</a>

        <a href="something" class="button1">Invest</a>
  <a href="something" class="button1">Sell</a>

  <h2>Stocks</h2>

  <table class="table table-hover table-dark">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Code</th>
      <th scope="col">Company</th>
      <th scope="col">PurchasedPrice</th>
      <th scope="col">Units</th> 
    </tr>
  </thead>
  {{#each stk}}
  {{#unless this.dataValues.Watched}}
    <tr>
      <th scope="row">{{this.dataValues.id}}</th>
      <td>{{this.dataValues.Code}}</td>
      <td>{{this.dataValues.Company}}</td>
      <td>{{this.dataValues.PurchasePrice}}</td>
      <td>{{this.dataValues.Units}}</td>
    </tr>
    {{/unless}}
  {{/each}}
  </tbody>
</table>

  <h2>Watched Stocks</h2>

  <table class="table table-hover table-dark">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Code</th>
      <th scope="col">Company</th>
      <th scope="col">WatchedPrice</th>
    </tr>
  </thead>
  {{#each stk}}
  {{#if this.dataValues.Watched}}
    <tr>
      <th scope="row">{{this.dataValues.id}}</th>
      <td>{{this.dataValues.Code}}</td>
      <td>{{this.dataValues.Company}}</td>
      <td>{{this.dataValues.PurchasePrice}}</td>
    </tr>
    {{/if}}
  {{/each}}
  </tbody>
</table>

      </div>
    </div>
  </div>
    <!-- Right Side -->
    {{!-- {{> rightSide}} --}}
  </div>
</div>

 